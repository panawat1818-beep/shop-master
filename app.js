const state={products:[]};const $=s=>document.querySelector(s);const $$=s=>document.querySelectorAll(s);const login=$('#loginScreen'),app=$('#appScreen'),modal=$('#modal'),list=$('#productList');function render(){list.innerHTML=state.products.length?state.products.map(p=>`<article class="product"><b>${p.name}</b><small>${p.store} · ${p.url}</small></article>`).join(''):'<p class="empty">ยังไม่มีสินค้า เพิ่มสินค้าด้วย URL ได้เลย</p>'}function show(view){$$('.view').forEach(x=>x.classList.toggle('active',x.id===view));$$('.nav').forEach(x=>x.classList.toggle('active',x.dataset.view===view));if(view==='home')$$('.view').forEach(x=>x.classList.remove('active'))}function open(){modal.classList.remove('hidden')}function close(){modal.classList.add('hidden');$('#productForm').reset()}$('#loginForm').addEventListener('submit',e=>{e.preventDefault();login.classList.remove('active');app.classList.add('active');render()});$('#logout').addEventListener('click',()=>{app.classList.remove('active');login.classList.add('active')});$('#openAdd').onclick=open;$('#openAdd2').onclick=open;$('#closeModal').onclick=close;$('#productForm').addEventListener('submit',e=>{e.preventDefault();state.products.unshift({name:$('#name').value.trim(),url:$('#url').value.trim(),store:$('#store').value});render();close();show('products')});$$('[data-view]').forEach(b=>b.addEventListener('click',()=>show(b.dataset.view)));if('serviceWorker'in navigator)window.addEventListener('load',()=>navigator.serviceWorker.register('./sw.js').catch(()=>{}));render();
